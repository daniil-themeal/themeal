# Server / Docker config (copy to .env on VPS)
TELEGRAM_BOT_TOKEN=
CURSOR_API_KEY=
ALLOWED_USER_IDS=

# On server always use cloud — local needs Cursor on the same machine
AGENT_MODE=cloud

CLOUD_REPO_URL=https://github.com/daniil-themeal/themeal.git
CLOUD_REPO_REF=feature/next

CURSOR_MODEL=composer-2.5
