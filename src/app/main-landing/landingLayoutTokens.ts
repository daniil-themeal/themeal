import type { CSSProperties } from 'react';

import { CHECKOUT_CARD_PADDING_CLAMP } from '../components/checkout/checkoutSpacing';

export const landingLayoutStyle: CSSProperties = {
  '--checkout-card-padding': CHECKOUT_CARD_PADDING_CLAMP,
};
