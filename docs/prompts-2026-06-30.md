# Промпты и правки — 30 июня 2026

Документ собран по git-коммитам, деплоям и чат-сессиям за день.  
Production: https://themeal-test.vercel.app  
Ветка: `feature/next`

---

## Сводка коммитов

| Время | Коммит | Статус | Файлы |
|-------|--------|--------|-------|
| 12:09 | `dc45abe` | задеплоен | `CheckoutPage.tsx`, удалён `usePlanStepScrollChaining.ts` |
| 12:59 | `b48376b` | задеплоен | `CheckoutPage.tsx`, `OrderSummary.tsx`, `TrialOrderSummary.tsx`, `checkoutSpacing.ts` |
| 13:33 | `3321ea6` | задеплоен | `FullMenuPanel.tsx` |

**Не попало в прод (отменено):** выравнивание «остановки» sticky summary по макету (`top: 16px`, перенос sticky на inner wrapper) — после реализации пользователь написал «отмена», изменения откатили.

---

## Блок 1. Скролл checkout plan → native sticky

**Коммит:** `dc45abe`  
**Исходные промпты (сессия «скролл в плане»):**

```
давай поработаем над скролом в плане
```

```
чекаут план
```

```
посмотри как сделано в этом макете, определи @c:\Users\Bender\Downloads\Telegram Desktop\meals_plan_step_v2.html
```

```
A
```

```
+
```

```
деплой
```

### Промпт для повторного внедрения

```
Упрости скролл на шаге Plan в checkout: убери JS scroll chaining, оставь нативный sticky как в макете meals plan.

### Контекст
На desktop (≥768px) шаг plan использует кастомный хук scroll chaining между левой колонкой (PlanSelector, Days, Duration…) и правой (OrderSummary). Хук перехватывает wheel, делает smooth scroll через requestAnimationFrame и связывает scrollTop двух контейнеров.

Нужно перейти на модель из HTML-макета meals_plan_step_v2.html:
- один scroll container (`bodyRef` / main scroll area checkout)
- правая колонка — `position: sticky`
- без inner scroll у summary
- без JS wheel handlers для chaining

Mobile не трогать: один вертикальный скролл, BottomFloatTotalBlock, IntersectionObserver для summary — как есть.

### Что сделать

1. Удалить файл `usePlanStepScrollChaining.ts`.

2. В `CheckoutPage.tsx`:
   - убрать import и вызов `usePlanStepScrollChaining({ enabled: isOpen && checkoutStep === 'plan', bodyRef, rightRef })`

3. Правая колонка OrderSummary (`ref={rightRef}`):
   - оставить: `md:sticky md:top-0 md:self-start`, padding, width constraints
   - убрать с desktop: `md:max-h-[calc(100svh-56px)]`, `md:min-h-0`, `md:overflow-x-hidden`, `md:overflow-y-hidden`

4. Не менять: `bodyRef` с `overflow-y-auto`, grid layout, BottomFloatTotalBlock, handleScrollToSummary, IntersectionObserver, горизонтальный скролл карусели блюд.

### Проверка
- desktop: один нативный скролл над обеими колонками
- summary липнет при прокрутке
- mobile без регрессий
- `npm run build` проходит
```

---

## Блок 2. Выравнивание правой колонки с плашкой Light

**Входит в коммит:** `b48376b` (незакоммиченная работа до деплоя)  
**Исходные промпты:**

```
увеличь в правой колонке от верху отступ так, чтобы она была на одном уровне с плашкой light
```

```
сильно много добавил
```

### Промпт для повторного внедрения

```
На desktop checkout plan step опусти правую колонку (OrderSummary), чтобы верх карточки summary был на одном уровне с первой plan-карточкой (Light), а не с заголовком «Choose your plan».

1. В `checkoutSpacing.ts` добавь:
   `CHECKOUT_PLAN_SUMMARY_ALIGN_OFFSET_CLAMP = '20px'`
   (только gap между заголовком PlanSelector и карточками, без высоты заголовка — первая итерация с title+gap оказалась слишком большой)

2. В `CheckoutPage.tsx` для правой колонки (не trial):
   - style: `'--checkout-plan-summary-align-offset': CHECKOUT_PLAN_SUMMARY_ALIGN_OFFSET_CLAMP`
   - className: `md:pt-[calc(56px+var(--checkout-plan-summary-align-offset))]`

3. Для trial оставить `md:pt-[56px]` — там нет плашки Light.

Mobile (`max-md:pt-0`) не менять.
```

---

## Блок 3. Order Summary — горизонтальные отступы −25%

**Входит в коммит:** `b48376b`  
**Исходный промпт:**

```
уменьши боковые отступы в правой колонке внутри каждой секции на 25%, чтобы при этом были значения кратные 4
```

### Промпт для повторного внедрения

```
Уменьши горизонтальные отступы внутри секций Order Summary / TrialOrderSummary на 25%. Значения кратные 4px. Не трогай глобальный CHECKOUT_CARD_PADDING_CLAMP.

1. В checkoutSpacing.ts:
   CHECKOUT_ORDER_SUMMARY_SECTION_PADDING_CLAMP =
   'clamp(16px, calc(16px + (100vw - 20rem) * 8 / 448), 24px)'
   (было 20→32px)

2. OrderSummary.tsx и TrialOrderSummary.tsx:
   - CSS-переменная `--order-summary-section-padding`
   - секции: px-[length:var(--order-summary-section-padding)]
   - fade width карусели и CheckoutScrollEdgeGutter — тот же токен
   - carousel px в TrialOrderSummary — тот же токен

Full-bleed dividers, delivery/payment, FullMenuPanel не менять.
```

---

## Блок 4. Order Summary — вертикальные отступы −25%

**Входит в коммит:** `b48376b`  
**Исходный промпт:**

```
теперь вертикальные тоже
```

### Промпт для повторного внедрения

```
По аналогии с горизонтальными (−25%, кратные 4) уменьши вертикальный ритм только в OrderSummary / TrialOrderSummary.

Добавить в checkoutSpacing.ts:
- CHECKOUT_ORDER_SUMMARY_CARD_PADDING_Y_CLAMP = '20px'        (было 28px)
- CHECKOUT_ORDER_SUMMARY_CARD_PADDING_Y_MOBILE_CLAMP = '12px'  (было 16px)
- CHECKOUT_ORDER_SUMMARY_SECTION_GAP_CLAMP =
  'clamp(20px, calc(20px + (100vw - 20rem) * 4 / 448), 24px)' (было 24→32)
- CHECKOUT_ORDER_SUMMARY_INNER_GAP_MD_CLAMP = '12px'           (было 16px)
- CHECKOUT_ORDER_SUMMARY_INNER_GAP_SM_CLAMP = '4px'            (было 8px)

В OrderSummary / TrialOrderSummary:
- card pt/pb через CSS vars
- section-gap через CHECKOUT_ORDER_SUMMARY_SECTION_GAP_CLAMP
- inner flex gaps 16px→12px, 8px→4px через vars

Не трогать: scroll-mb-[72px], --checkout-plan-column-pb-mobile, глобальный CHECKOUT_SECTION_GAP_CLAMP.
```

---

## Блок 5. Левая колонка plan — padding-top 40px на md+

**Входит в коммит:** `b48376b`  
**Исходный промпт (browser preview / CSS change):**

```
Apply these CSS style changes to the codebase. These changes were made in a browser preview and need to be persisted to the source files:

Change 1 — CheckoutPage, left plan column div.flex:
  property: padding-top
  oldValue: 56px
  newValue: 40px
  classes: ... md:pt-[56px] ...
  → заменить на md:pt-[40px]
```

### Промпт для повторного внедрения

```
В CheckoutPage.tsx на шаге plan у левой колонки (PlanSelector, Ingredients, Days, Duration):
заменить md:pt-[56px] на md:pt-[40px].

Mobile padding через --checkout-step-header-pt не менять.
```

---

## Блок 6. Full Menu — отступ под хедером

**Коммит:** `3321ea6`  
**Исходный промпт:**

```
в окне фулл меню пропал отступ между хедером и блюдами
```

```
деплой
```

### Промпт для повторного внедрения

```
В FullMenuPanel.tsx пропал отступ между ModalHeader «Full menu» и контентом ниже.

Причина: у блока day picker было pt-[12px], случайно заменили на pt-0.

Исправление:
1. Блок day picker: className ... pt-[12px] pb-0 (вместо pt-0)
2. Для trial (day picker скрыт через display:none): добавить pt-[12px] на meals outer container когда isTrial && !isEmbedded

Файл: src/app/components/checkout/FullMenuPanel.tsx
```

---

## Блок 7. Деплои

**Промпты:**

```
деплой
```

(использовался дважды — после `b48376b` и после `3321ea6`)

### Промпт для деплоя

```
Закоммить изменения checkout/full menu, прогнать npm run build, push в feature/next.
Деплой идёт через GitHub Actions workflow vercel-production.yml на https://themeal-test.vercel.app
```

---

## Отменённая работа (не включать в перенос)

**Промпты:**

```
посмотри внимательно как останавливается правая колонка
```

```
посмотри внимательно как останавливается правая колонка в загруженном файле
```

```
Выравнивание «остановки» правой колонки с макетом — Implement the plan...
```

```
отмена
```

Sticky summary stop (перенос sticky на inner wrapper, `top: 16px`) **откатили** — в prod только native sticky из блока 1 без этих правок.

---

## Чеклист: всё ли включено

- [x] Native sticky scroll, удаление `usePlanStepScrollChaining` — `dc45abe`
- [x] Align offset правой колонки с Light (+20px) — `b48376b`
- [x] Order summary horizontal padding −25% — `b48376b`
- [x] Order summary vertical spacing −25% — `b48376b`
- [x] Левая колонка `md:pt-[40px]` — `b48376b`
- [x] Full Menu header gap `pt-[12px]` — `3321ea6`
- [x] Деплои на production (2 раза)
- [ ] Sticky stop alignment по макету — **отменено, не в prod**

---

## Файлы, затронутые за день

```
src/app/components/checkout/CheckoutPage.tsx
src/app/components/checkout/OrderSummary.tsx
src/app/components/checkout/TrialOrderSummary.tsx
src/app/components/checkout/checkoutSpacing.ts
src/app/components/checkout/FullMenuPanel.tsx
src/app/components/checkout/usePlanStepScrollChaining.ts  (удалён)
```

---

## Один сводный промпт (для другого репозитория)

```
Внеси все задеплоенные правки checkout plan step и full menu от 30.06.2026:

1) PLAN SCROLL: удали usePlanStepScrollChaining, у правой колонки убери md:max-h/min-h/overflow-y-hidden, оставь md:sticky md:top-0.

2) PLAN LAYOUT: левая колонка md:pt-[40px]; правая (не trial) md:pt-[calc(56px+20px)] через CHECKOUT_PLAN_SUMMARY_ALIGN_OFFSET_CLAMP='20px'.

3) ORDER SUMMARY TOKENS в checkoutSpacing.ts:
   - SECTION_PADDING: clamp(16px…24px)
   - CARD_PADDING_Y: 20px, mobile 12px
   - SECTION_GAP: clamp(20px…24px)
   - INNER_GAP_MD: 12px, INNER_GAP_SM: 4px
   Подключи в OrderSummary.tsx и TrialOrderSummary.tsx (section px, card py, dividers, carousel gutter/fade).

4) FULL MENU: FullMenuPanel — pt-[12px] под хедером у day picker; для trial pt-[12px] на meals outer.

Не трогай отменённый sticky stop alignment (top:16px на inner wrapper).

npm run build, commit, push feature/next.
```
